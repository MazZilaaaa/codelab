*Run flask app*
'$ export FLASK_APP=hello.py'
'$ flask run'

*Killing flask proccess*
'lsof -i:PORT'
'kill -9 PID'

*Externally Visible Server*
'$ flask run --host=0.0.0.0'

*Debug mode*
'$ export FLASK_ENV=development'
'$ flask run'